// import the modules for the User, Pet, Need, and Species models
const User = require("./User");
const Pet = require("./Pet");
const Species = require("./Species");

// export the models for them to be used elsewhere
module.exports = { User, Pet, Species };
